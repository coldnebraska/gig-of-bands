<div class="container mx-auto flex justify-center items-center p-6">
        <div class="profile-header max-w-md bg-slate-400 p-6 rounded-md shadow-md">
            <div class="text-center">
                {{#if myprofile}}
                <h1>My Profile</h1>
                {{/if}}
                {{#if band}}
                <h2 class="text-2xl text-white font-bold">{{band.name}}</h2>
                <p class="text-white">Genre: {{band.genre}}</p>
                {{/if}}
                {{#if venue}}
                <h2 class="text-2xl text-white font-bold">{{venue.name}}</h2>
                {{/if}}
            </div>
        </div>
    </div>
    {{#if myprofile}}
        <h1>Add Event</h1>
    {{/if}}

<div id="calendar" style="max-width: 1000px; margin:0 auto"></div>

<script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.10/index.global.min.js'></script>
<script src="/js/profile.js"></script>